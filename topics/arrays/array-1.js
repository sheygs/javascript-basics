// Are zero-based index i.e. starting at zero
// used for holding an item where position matters
// creating an array  - [] or new Array()
//getting the length of an array
// accessing arrays - using their indexes
//mutable/immutable methods

// creating an array
let list = new Array(5);
const names = ['Olusegun', 'John', 'Adebisi'];

//getting the length of an array
const len = names.length;

// accessing items in an arrays
const firstName = names[0];
const lastName = names[len - 1];

